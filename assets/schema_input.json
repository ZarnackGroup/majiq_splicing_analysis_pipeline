{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/ZarnackGroup/majiq_splicing_analysis_pipeline/master/assets/schema_input.json",
    "title": "ZarnackGroup/majiq_splicing_analysis_pipeline pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "genome_bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "BAM file path must be provided, cannot contain spaces, and must end in '.bam'"
            },
            "condition": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Condition must be provided and contain syntactically valid names.",
                "meta": ["condition"]
            }
        },
        "required": ["sample", "genome_bam", "condition"]
    }
}
